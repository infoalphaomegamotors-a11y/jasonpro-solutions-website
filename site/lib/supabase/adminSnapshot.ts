import type { AdminSnapshot } from "./types";

export const unconfiguredAdminSnapshot: AdminSnapshot = {
  products: null,
  orders: null,
  briefs: null,
  activeProjects: null,
  members: null,
  source: "unconfigured",
};
